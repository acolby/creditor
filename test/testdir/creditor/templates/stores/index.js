
const state = {};

const actions = {
  fetch: async () => {
    // setState
    return state;
  },
}

export const CREDITOR_UNDERSCORE_NAME = {
  actions,
  state,
}

export default CREDITOR_UNDERSCORE_NAME;
